# A sample Guardfile
# More info at https://github.com/guard/guard#readme

guard 'livereload' do
  watch("index.html")
  watch(%r{css/.+\.(sass|css)})
  watch(%r{img/.+\.(jpg|png)})
  watch(%r{js/.+\.(js|coffee)})
end
